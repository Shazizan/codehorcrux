#creating a new table in postgres

CREATE TABLE pgtest (
testval VARCHAR(10)
);

#Insert the value
INSERT INTO pgtest (testval)
VALUES ('Hello');

#Execute the select statement
SELECT testval
FROM pgtest;
